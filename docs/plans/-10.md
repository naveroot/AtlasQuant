# Plan #10: Миграция тестов Minitest → RSpec + SimpleCov

Plane issue: `97e8b639-a65c-43fb-bda9-81f072e006b0`

**Status:** active

## Scope

Подключить rspec-rails и simplecov, перенести 11 Minitest-файлов в `spec/`, настроить порог 80% для models/services/controllers, обновить CI, удалить `test/`.

## Grounding (GATE_2)

| Проверка | Результат |
|----------|-----------|
| Minitest files in `test/` | 11 files (models, controllers, helpers, services) |
| `spec/` exists | нет — создать через `rspec:install` |
| `Gemfile` rspec/simplecov | нет — добавить |
| `config/ci.rb` | `bin/rails test` → заменить на `bundle exec rspec` |
| `.github/workflows/ci.yml` | `bin/rails db:test:prepare test` → RSpec |
| Fixtures | `test/fixtures/users.yml` → `spec/fixtures/users.yml` |
| System tests | только `application_system_test_case.rb` — заменить на `spec/system` support |
| App code for coverage | 1 model, 5 controllers, 5 services — покрыты существующими тестами + доп. specs для gaps |

**GATE_2: PASS**

## Implementation Checklist

- [x] AC-1: Gems + `rspec:install` + migrate all specs
- [x] AC-2: SimpleCov config with 80% group thresholds
- [x] AC-3: Update `config/ci.rb`
- [x] AC-4: Update `.github/workflows/ci.yml`
- [x] AC-5: Delete `test/`
- [x] AC-6: `bin/ci` green

## Evidence Log

| AC | Evidence | Status |
|----|----------|--------|
| AC-1 | `spec/**/*.rb`, `bundle exec rspec` exit 0 (44 examples) | done |
| AC-2 | `spec/spec_helper.rb`, group coverage ≥80% | done |
| AC-3 | `config/ci.rb` | done |
| AC-4 | `.github/workflows/ci.yml` | done |
| AC-5 | `test/` removed | done |
| AC-6 | `bin/ci` exit 0 | done |

## GATE_4: PASS

All AC have file/test evidence in diff.
