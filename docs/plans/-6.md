# Plan #6: Add trading instruments to favorites

Plane issue: `4211ae9c-67f7-45e7-a1f6-3ae42e1d7a1b`

**Status:** active

## Scope

Персональный список избранных MOEX-инструментов для авторизованных пользователей: модель `Favorite`, сервисы add/remove, контроллер, UI на страницах instruments и отдельная страница `/favorites`.

## Grounding (GATE_2)

| Проверка | Результат |
|----------|-----------|
| `User` model + auth (`require_login`) | есть |
| `Moex::CurrencyFutures::List` с `Instrument` struct | есть |
| `InstrumentsController` index/show | есть |
| Service pattern (`Users::Register`, `Sessions::Authenticate`) | есть |
| `Instrument` AR model | нет — храним только `secid` |
| RSpec | нет — Minitest per spec AC-7 |
| Fixtures `users.yml` | есть |

**GATE_2: PASS**

## Implementation Checklist

- [x] AC-1: migration + `Favorite` model + `User has_many :favorites`
- [x] AC-2: `app/services/favorites/add.rb`
- [x] AC-3: `app/services/favorites/remove.rb`
- [x] AC-4: `FavoritesController`, routes, `require_login`
- [x] AC-5: favorite toggle UI on `instruments/index` and `instruments/show`; nav link
- [x] AC-6: `favorites/index` view
- [x] AC-7: Minitest (model, services, controller)
- [x] AC-8: `bin/ci` green

## Evidence Log

| AC | Evidence | Status |
|----|----------|--------|
| AC-1 | `db/migrate/20260629200000_create_favorites.rb`, `app/models/favorite.rb`, `test/models/favorite_test.rb` | done |
| AC-2 | `app/services/favorites/add.rb`, `test/services/favorites/add_test.rb` | done |
| AC-3 | `app/services/favorites/remove.rb`, `test/services/favorites/remove_test.rb` | done |
| AC-4 | `app/controllers/favorites_controller.rb`, `config/routes.rb`, `test/controllers/favorites_controller_test.rb` | done |
| AC-5 | `app/views/instruments/_favorite_toggle.html.erb`, `index.html.erb`, `show.html.erb`, `application.html.erb` | done |
| AC-6 | `app/views/favorites/index.html.erb` | done |
| AC-7 | 4 test files, 56 tests green | done |
| AC-8 | `bin/ci` exit 0 | done |

## GATE_4: PASS

All AC have file/test evidence in diff.
