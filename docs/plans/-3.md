# Plan -3: Заполнить README.md

Plane issue: `9517f05d-354e-4414-aece-35a4e46c1f93`

**Status:** active

## Scope

Заменить Rails-заглушку в корневом `README.md` на проектную документацию для разработчиков. Контент основан на `AGENTS.md` и фактическом состоянии репозитория. Без изменений кода приложения.

## Grounding (Gate 2)

| Проверка | Результат |
|----------|-----------|
| `README.md` существует в корне | PASS — placeholder Rails |
| `AGENTS.md` — источник фактов о продукте/стеке | PASS |
| Ruby 3.2.11 (`.ruby-version`, `mise.toml`) | PASS |
| PostgreSQL 16 (`mise.toml`, `config/database.yml`) | PASS |
| Solid stack (`solid_cache`, `solid_queue`, `solid_cable` в Gemfile) | PASS |
| Auth MVP реализован (User, sessions, registrations) | PASS — plan #1 complete |
| Instrument/FundingCalculator не реализованы | PASS — описать как planned MVP |
| `bin/dev`, `bin/setup`, `bin/ci`, `bin/jobs` существуют | PASS |
| Minitest активен (`test/`), RSpec — целевой | PASS |
| Dockerfile + `config/deploy.yml` (Kamal) | PASS |

**GATE_2: PASS**

## Implementation Steps

- [x] Step 1: Написать `README.md` — заголовок, о проекте, стек, prerequisites
- [x] Step 2: Добавить getting started (`mise install`, `bin/setup`, `bin/dev`)
- [x] Step 3: Добавить testing (`bin/rails test`, `bin/ci`), background jobs (`bin/jobs`), deployment (Docker/Kamal)
- [x] Step 4: Добавить ссылки на `AGENTS.md`, `docs/`, текущий статус MVP
- [x] Step 5: Удалить Rails-заглушку; self-check AC-1..AC-6

## Exit Criteria

- [x] All spec AC checked
- [x] `bin/ci` passes (tests, lint, audits; Brakeman EOLRuby — pre-existing on main)
- [x] Spec-conformance review done (GATE_4: PASS)

## Evidence Log

| Step | Evidence | Status |
|------|----------|--------|
| README content | `README.md` | done |
| AC verification | grep AC-1..AC-6 PASS | done |
| CI | setup, rubocop, audits, 26 tests PASS | done |
