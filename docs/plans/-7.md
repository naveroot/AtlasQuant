# Plan #7: Instrument charts display and UX

Plane issue: `67c5d11f-a60f-4628-ba0b-5e2e89016748`

**Status:** active

## Scope

Исправить отображение графиков на странице инструмента, заменить Chart.js на Lightweight Charts (candlestick), убрать OHLC-таблицу, улучшить UX списка и детальной страницы.

## Root Cause (Grounding)

| Проблема | Детали |
|----------|--------|
| Broken data attrs | `@candles.map(...).to_json` в raw HTML `data-*` ломает атрибут (незакрытые `"`) |
| Wrong chart type | Line chart close-only; OHLC данные лучше как candlestick |
| UX | Таблица цифр на show; plain table на index |

## Grounding (GATE_2)

| Проверка | Результат |
|----------|-----------|
| `InstrumentsController`, MOEX services | есть (#4) |
| Stimulus + importmap | есть |
| `price_chart_controller.js` + Chart.js pin | есть, заменить на Lightweight Charts |
| Minitest controller tests | есть |
| RSpec | нет — Minitest per spec |

**GATE_2: PASS**

## Implementation Checklist

- [x] AC-1: Pin Lightweight Charts; rewrite `price_chart_controller.js` (candlestick series)
- [x] AC-2: `show.html.erb` — `tag.div` data helpers для JSON; container sizing
- [x] AC-3: Summary cards helper/partial; remove OHLC table
- [x] AC-4: Redesign `index.html.erb` as card grid
- [x] AC-5: Period presets (7/30/90); TradingView attribution
- [x] AC-6: Update controller tests; `bin/ci` green

## Evidence Log

| AC | Evidence | Status |
|----|----------|--------|
| AC-1 | `config/importmap.rb`, `price_chart_controller.js` | done |
| AC-2 | `app/views/instruments/show.html.erb` | done |
| AC-3 | `show.html.erb`, `app/helpers/instruments_helper.rb` | done |
| AC-4 | `app/views/instruments/index.html.erb` | done |
| AC-5 | `show.html.erb` presets + attribution | done |
| AC-6 | `test/controllers/instruments_controller_test.rb`, `test/helpers/instruments_helper_test.rb`, `bin/ci` | done |

## GATE_4: PASS

All AC have file/test evidence in diff.
