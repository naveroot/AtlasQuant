# Plan #11: Актуализировать AGENTS.md под фактическое состояние кода

Plane issue: `e13ee299-50ee-4f74-8660-4912a49393a6`

**Status:** active

## Scope

Обновить `AGENTS.md` и `docs/index.md`: отразить реализованное (auth, MOEX, charts, CI/deploy) и backlog (Instrument AR, BasisCalculator, Dashboard). Docs-only, без изменений приложения.

## Grounding (GATE_2)

| Проверка | Результат |
|----------|-----------|
| `User` + auth | `app/models/user.rb`, `SessionsController`, `RegistrationsController`, `Sessions::Authenticate`, `Users::Register` (#1) |
| MOEX integration | `app/services/moex/client.rb`, `currency_futures/list.rb`, `historical_candles.rb` (#4) |
| Charts UI | `app/javascript/controllers/price_chart_controller.js`, `InstrumentsController` (#4, #7) |
| `Instrument` AR model | **нет** — только value object в `Moex::CurrencyFutures::List` |
| `BasisCalculator` | **нет** — ADR-001 зафиксировал направление (#9) |
| `DashboardController` | **нет** — есть `HomeController` placeholder |
| Deploy/CI | `config/deploy.yml`, `.github/workflows/ci.yml`, `bin/ci` (#2) |
| Tests | Minitest в `test/` (13 файлов); RSpec `spec/` отсутствует |
| AGENTS.md stale sections | «Доменные сущности — ещё не реализованы»; MVP auth unchecked |

**GATE_2: PASS**

## Implementation Checklist

- [x] AC-1: Раздел «Доменные сущности» — implemented vs planned
- [x] AC-2: MVP checkboxes — auth, MOEX, CI ✅; basis, watchlist, RSpec ⬜
- [x] AC-3: Controllers/services tables — фактические файлы + backlog
- [x] AC-4: Backlog section — ссылки на Plane #1, #2, #4, #7, #9 и открытые направления
- [x] AC-5: `docs/index.md` — ссылка на AGENTS.md как статус MVP
- [x] AC-6: `bin/ci` green

## Evidence Log

| AC | Evidence | Status |
|----|----------|--------|
| AC-1 | `AGENTS.md` — таблица «Доменные сущности» с колонками «Статус» и «Evidence» | done |
| AC-2 | `AGENTS.md` — «Границы MVP» с `[x]` для auth/MOEX/CI | done |
| AC-3 | `AGENTS.md` — «Ориентиры по реализации» с HomeController, Moex::* | done |
| AC-4 | `AGENTS.md` — раздел «Backlog (Plane)» | done |
| AC-5 | `docs/index.md` — «актуальный статус реализации» | done |
| AC-6 | `bin/ci` exit 0 (40 tests, 12.26s) | done |

## GATE_4: PASS

All AC have file/evidence in diff.
