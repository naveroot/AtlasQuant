# Plan #5: Plane agent account

Plane issue: `ec0b624a-d3cd-4f0f-9140-ecd074574532`

**Status:** active

## Scope

Отдельный API-ключ пользователя-агента в Plane, документация по созданию учётной записи, использование ключа в orchestrator и Supercode scripts, роли в комментариях, verify-скрипт.

## Grounding (GATE_2)

| Проверка | Результат |
|----------|-----------|
| `PlaneClient.addComment` | `.orchestrator/src/plane-client.ts` — POST comments |
| `poll-plane.ts`, `start-agent.ts` | используют `PLANE_API_KEY` для всех операций |
| `update-plane-state.sh` | `PLANE_API_KEY` для PATCH + POST comment |
| `.env.example` files | только `PLANE_API_KEY`, нет agent key |
| Plane API `/users/me/` | документирован Plane API для проверки identity |
| `setup:states` | admin key — оставляем на `PLANE_API_KEY` |
| bin/ci | Rails tests only — orchestrator не в CI (OK) |

**GATE_2: PASS**

## Implementation Checklist

- [x] AC-1: `PLANE_AGENT_API_KEY` in `.orchestrator/.env.example` and `.supercode/.../.env.example`
- [x] AC-2: `docs/agent-pipeline/plane-agent-account.md`
- [x] AC-3: `plane-auth.ts` + update poll-plane, start-agent, update-plane-state.sh
- [x] AC-4: `formatAgentComment(role, message)` + role mapping in calls
- [x] AC-5: `verify-agent-account.ts` + `npm run verify:agent`
- [x] AC-6: `bin/ci` green

## Evidence Log

| AC | Evidence | Status |
|----|----------|--------|
| AC-1 | `.orchestrator/.env.example`, `.supercode/.../.env.example` | done |
| AC-2 | `docs/agent-pipeline/plane-agent-account.md` | done |
| AC-3 | `plane-auth.ts`, `poll-plane.ts`, `start-agent.ts`, `update-plane-state.sh` | done |
| AC-4 | `plane-auth.ts`, comment calls with roles | done |
| AC-5 | `verify-agent-account.ts`, `plane-auth.test.ts`, `npm test` | done |
| AC-6 | `bin/ci` exit 0 | done |

## Role Mapping

| Контекст | Роль в комментарии |
|----------|-------------------|
| Poller claim | Orchestrator |
| Cloud agent start/finish/error | Cloud Agent |
| `update-plane-state.sh spec_review` | Spec Review |
| `update-plane-state.sh grounding` | Grounding |
| `update-plane-state.sh implement` | Implement |
| `update-plane-state.sh review` | Review |
| `update-plane-state.sh blocked` | Blocked |
