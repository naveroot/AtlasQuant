# Plan #5: Plane agent service account

Plane issue: `ec0b624a-d3cd-4f0f-9140-ecd074574532`

**Status:** active

## Scope

Runbook для создания сервисного пользователя Plane, verify-скрипт, dual-key policy в orchestrator (admin vs agent), MCP и bash fallbacks. Роли SDD остаются в теле комментария (spec #8).

## Grounding (GATE_2)

| Проверка | Результат |
|----------|-----------|
| `PLANE_AGENT_API_KEY` в `.env.example` | есть (spec #8), расширить docs |
| `agent-clarification.sh` resolve_api_key | есть — agent preferred |
| `PlaneClient.addComment` | `.orchestrator/src/plane-client.ts` |
| `poll-plane.ts` / `start-agent.ts` | split via `plane-clients.ts` |
| `plane-mcp-server.sh` | prefers `PLANE_AGENT_API_KEY` |
| Plane `/users/me/` | verify script |
| Orchestrator tests | `npm test` 6 tests pass |
| `bin/ci` | exit 0 |

**GATE_2: PASS**

## Implementation Checklist

- [x] AC-1: `docs/agent-pipeline/plane-agent-account.md`
- [x] AC-2: `scripts/verify-plane-agent.sh`
- [x] AC-3: `resolvePlaneAgentApiKey()` + `config.test.ts`
- [x] AC-4: dual client in `poll-plane.ts`, `start-agent.ts`, `plane-clients.ts`
- [x] AC-5: `plane-mcp-server.sh`, `plane-mcp-context.sh`
- [x] AC-6: `update-plane-state.sh`, `agent-clarification.sh` split keys
- [x] AC-7: `.env.example` updates + link to runbook
- [x] AC-8: `npm test` + `bin/ci`

## Evidence Log

| AC | Evidence | Status |
|----|----------|--------|
| AC-1 | `docs/agent-pipeline/plane-agent-account.md` | done |
| AC-2 | `scripts/verify-plane-agent.sh` | done |
| AC-3 | `config.ts`, `config.test.ts` | done |
| AC-4 | `plane-clients.ts`, `poll-plane.ts`, `start-agent.ts` | done |
| AC-5 | `plane-mcp-server.sh`, `plane-mcp-context.sh` | done |
| AC-6 | `update-plane-state.sh`, `agent-clarification.sh` | done |
| AC-7 | `.orchestrator/.env.example`, `.supercode/.../.env.example` | done |
| AC-8 | `npm test` (6), `bin/ci` exit 0 | done |

## GATE_4: PASS

All AC have file/test evidence in diff.

## Post-merge (human)

1. Create Plane user **AtlasQuant Agent** per runbook
2. Set `PLANE_AGENT_API_KEY` in local `.env` files
3. Run `verify-plane-agent.sh --expect "AtlasQuant Agent"`
